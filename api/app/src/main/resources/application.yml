spring:
  application:
    name: maslul-api
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    open-in-view: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
